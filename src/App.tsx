import AppRouter from './route/router';

/**
 * The main App component.
 * Its only job is to render the router, which handles all page logic.
 */
function App() {
  return <AppRouter />;
}

export default App;
